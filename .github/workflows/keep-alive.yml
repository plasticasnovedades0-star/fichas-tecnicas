name: Keep Supabase Alive

on:
  schedule:
    # Se ejecuta cada 3 días a las 00:00 UTC
    - cron: '0 0 */3 * *'
  workflow_dispatch: # Permite ejecución manual desde la interfaz de GitHub Actions

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Supabase API
        run: |
          curl -i \
            -H "apikey: sb_publishable_oN67frGtXxOa9qt4oC1h9A_pkNEBGwt" \
            -H "Authorization: Bearer sb_publishable_oN67frGtXxOa9qt4oC1h9A_pkNEBGwt" \
            "https://fnhqpqnretooavpsobri.supabase.co/rest/v1/fichas?limit=1"
